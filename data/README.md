# data

这里存放本机全部棋路数据（开局棋路、中局/残局案例、文件夹、笔记），目前是一个 `ttzchess.sqlite3` 文件。

换电脑同步：关掉 TtzChess → 把整个 `data` 文件夹拷到 U 盘 → 在新电脑同一位置（开发时是项目根目录下，安装/解压版是 `TtzChess.exe` 旁边）替换掉原来的 `data` → 再打开程序。

替换是整份覆盖，不会自动合并两台电脑各自新增的棋路。
