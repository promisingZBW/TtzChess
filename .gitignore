node_modules
out
dist
dist-electron
release
.env
.env.*
*.log
.DS_Store
.vscode/*
!.vscode/extensions.json

# 开发日志，只在本地记录进度，不进版本库
journal.md

# 调试用的临时截图
.debug-screenshot-*.png

# 阶段6：Pikafish引擎二进制+53MB的nnue神经网络权重文件，体积太大不适合进git仓库，
# 运行 `npm run setup:pikafish` 会自动下载解压到这里，见 resources/engines/README.md
/resources/engines/pikafish/
/resources/engines/.tmp-setup/

# 用户棋路数据：整夹拷贝即可同步到另一台电脑，不进版本库
/data/*
!/data/README.md
